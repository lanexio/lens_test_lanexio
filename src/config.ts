/**
 * Lens Protocol API 配置
 */

/**
 * Lens Protocol 主网 GraphQL API 端点
 */
export const LENS_API_ENDPOINT = 'https://api-v2.lens.dev';

/**
 * Lens Protocol 测试网 GraphQL API 端点
 */
export const LENS_TESTNET_API_ENDPOINT = 'https://api-v2-amoy.lens.dev';

/**
 * 默认使用的 API 端点
 */
export const DEFAULT_API_ENDPOINT = LENS_API_ENDPOINT;

/**
 * GraphQL 请求的默认超时时间（毫秒）
 */
export const DEFAULT_TIMEOUT = 30000;

/**
 * 批量请求的最大账户数量
 */
export const MAX_BATCH_SIZE = 50;
