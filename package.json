{
  "name": "lens_test_lanexio",
  "version": "1.0.0",
  "description": "Lens Protocol ML Score Fetcher - 获取指定用户ID列表的ML评分",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "example": "npm run build && node dist/example.js",
    "demo": "npm run build && node dist/demo.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["lens", "protocol", "ml-score", "graphql"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "graphql": "^16.8.1",
    "graphql-request": "^6.1.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
